{
  "name": "projet-step",
  "version": "1.0.0",
  "description": "Construction Market ERP - Projet STEP",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run start:dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  }
}
